// Libraries
import styled from 'styled-components';

// Components
import { Flex } from '@/components/ui';

export const DataTableWrapper = styled(Flex)`
  /* Pagination */
  .ant-pagination.ant-table-pagination {
    padding: 0 16px;
  }
`;
